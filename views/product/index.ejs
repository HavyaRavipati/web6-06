<div class="container-fluid">
  <div class=row>
    <div class="col col-md-12">
      <a href="product/create" class="btn btn-primary new">
        Create a new Product
      </a>
      <h2>Recent Product entries</h2>
      <p>There are
        <%= products.query.length %> orders.
    </p>
    <% for ( let i = 0; i < products.query.length; i++) {   
      product = products.query[i];   %>
      <div class="panel panel-default">
        <div class="panel-heading"></div>
        <div class="text-muted pull-right"></div>
        <div class="panel-body">
          ID:
          <%=product._id %>
          <br> Product Key:
          <%=product.productKey %>
          <br> Product Description:
          <%=product.description %>
          <br> Unit Price:
          <%=product.unitPrice %>
          <br> MFD:
          <%=product.MFD %>
          <br> EXD:
          <%=product.EXD%>
          
          
    <br>
    <br>
    <a href="product/delete/<%= product._id %>" class="btn btn-danger"> Delete </a>
    <a href="product/details/<%= product._id %>" class="btn btn-info"> Details </a>
    <a href="product/edit/<%= product._id %>" class="btn btn-warning"> Edit </a>
  </div>
  </div>
    <% } %>
 
</div>
</div>
</div>

